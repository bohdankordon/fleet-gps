"use strict";
const crypto = require("node:crypto"); const net = require("node:net"); const http = require("node:http"); const path = require("node:path"); const { spawn } = require("node:child_process"); const { config } = require("dotenv"); const { Client } = require("pg");
function freePort() { return new Promise((resolve, reject) => { const server = net.createServer(); server.once("error", reject); server.listen(0, "127.0.0.1", () => { const address = server.address(); const port = typeof address === "object" && address ? address.port : 0; server.close((error) => error ? reject(error) : resolve(port)); }); }); }
function startTrap(onRequest) { return new Promise((resolve, reject) => { const server = http.createServer((_, response) => { onRequest(); response.statusCode = 503; response.end(); }); server.on("clientError", onRequest); server.once("error", reject); server.listen(0, "127.0.0.1", () => { const address = server.address(); resolve({ server, port: typeof address === "object" && address ? address.port : 0 }); }); }); }
function close(server) { return new Promise((resolve) => { if (!server?.listening) return resolve(); server.close(() => resolve()); }); }
async function waitFor(url) { for (let attempt = 0; attempt < 50; attempt += 1) { try { if ((await fetch(url)).ok) return; } catch {} await new Promise((resolve) => setTimeout(resolve, 250)); } throw new Error("service unavailable"); }
function stop(child) { return new Promise((resolve) => { if (!child || child.exitCode !== null) return resolve(); const timer = setTimeout(() => child.kill("SIGKILL"), 2_000); child.once("exit", () => { clearTimeout(timer); resolve(); }); child.kill("SIGTERM"); }); }
async function databaseSnapshot(client) {
  const definitions = [["vehicle", "vehicles", "id"], ["currentState", "vehicle_current_states", "vehicle_id"], ["dailyVehicleStat", "daily_vehicle_stats", "id"], ["vehiclePositionObservation", "vehicle_position_observations", "id"], ["vehiclePositionBackfillCheckpoint", "vehicle_position_backfill_checkpoints", "id"], ["alertEvaluationObservation", "alert_evaluation_observations", "id"], ["alertEvent", "alert_events", "id"], ["outbox", "alert_notification_outbox", "id"]];
  const rows = []; for (const [, table, order] of definitions) rows.push(await client.query(`SELECT * FROM "${table}" ORDER BY "${order}"`));
  return { counts: Object.fromEntries(definitions.map(([key], index) => [key, rows[index].rowCount])), digest: crypto.createHash("sha256").update(JSON.stringify(rows.map((result) => result.rows))).digest("hex") };
}
async function main() {
  let api; let web; let trap; let database; let externalRequests = 0; let result = null;
  config({ path: path.resolve(__dirname, "../../..", ".env"), quiet: true, override: false });
  try {
    if (!process.env.DATABASE_URL) throw new Error("configuration"); database = new Client({ connectionString: process.env.DATABASE_URL }); await database.connect();
    const candidate = (await database.query(`WITH ordered AS (SELECT vehicle_id, observed_at, valid, outdated, lag(observed_at) OVER (PARTITION BY vehicle_id, date_trunc('day', observed_at) ORDER BY observed_at, fix_fingerprint) AS previous_at FROM vehicle_position_observations), grouped AS (SELECT vehicle_id, date_trunc('day', observed_at) AS day, count(*)::int AS point_count, count(*) FILTER (WHERE previous_at IS NOT NULL AND observed_at - previous_at > interval '5 minutes')::int AS gap_count, count(*) FILTER (WHERE valid = false OR outdated = true)::int AS warning_count FROM ordered GROUP BY vehicle_id, date_trunc('day', observed_at)) SELECT vehicle_id::text, day, point_count, gap_count, warning_count FROM grouped WHERE point_count > 1 ORDER BY (gap_count > 0) DESC, point_count DESC LIMIT 1`)).rows[0];
    if (!candidate?.vehicle_id || !(candidate.day instanceof Date)) throw new Error("multi-point history required");
    const from = candidate.day.toISOString(); const to = new Date(candidate.day.getTime() + 24 * 60 * 60 * 1_000).toISOString(); const query = new URLSearchParams({ from, to }).toString(); const before = await databaseSnapshot(database);
    const apiPort = await freePort(); const webPort = await freePort(); trap = await startTrap(() => { externalRequests += 1; });
    const apiEnv = { ...process.env, HOST: "127.0.0.1", PORT: String(apiPort), SYNC_SCHEDULER_ENABLED: "false", ALERT_INGESTION_ENABLED: "false", TELEGRAM_NOTIFICATIONS_ENABLED: "false", EQUGPS_BASE_URL: `https://127.0.0.1:${trap.port}/api`, EQUGPS_WEB_BASE_URL: `https://127.0.0.1:${trap.port}`, EQUGPS_EMAIL: "smoke@example.test", EQUGPS_PASSWORD: "smoke-password" };
    api = spawn(process.execPath, [path.resolve(__dirname, "../../api/dist/main.js")], { env: apiEnv, stdio: "ignore" }); await waitFor(`http://127.0.0.1:${apiPort}/api/health`);
    web = spawn(process.execPath, [require.resolve("next/dist/bin/next"), "start", "-p", String(webPort), "-H", "127.0.0.1"], { cwd: path.resolve(__dirname, ".."), env: { ...process.env, API_INTERNAL_BASE_URL: `http://127.0.0.1:${apiPort}` }, stdio: "ignore" }); await waitFor(`http://127.0.0.1:${webPort}/`);
    const trackPath = `/vehicles/${candidate.vehicle_id}/track?${query}`;
    const nest = await fetch(`http://127.0.0.1:${apiPort}/api/vehicles/${candidate.vehicle_id}/track?${query}`); const nestBody = await nest.json();
    const bff = await fetch(`http://127.0.0.1:${webPort}/api/vehicles/${candidate.vehicle_id}/track?${query}`); const bffBody = await bff.json();
    const trackPage = await fetch(`http://127.0.0.1:${webPort}${trackPath}`); const trackHtml = await trackPage.text();
    const details = await fetch(`http://127.0.0.1:${webPort}/vehicles/${candidate.vehicle_id}`); const detailsHtml = await details.text();
    const statuses = { nestTrack: nest.status, bffTrack: bff.status, trackPage: trackPage.status, details: details.status, map: (await fetch(`http://127.0.0.1:${webPort}/map`)).status, events: (await fetch(`http://127.0.0.1:${webPort}/events`)).status, root: (await fetch(`http://127.0.0.1:${webPort}/`)).status };
    const after = await databaseSnapshot(database); const pointCount = Number(bffBody?.summary?.pointCount); const gaps = Array.isArray(bffBody?.points) ? bffBody.points.slice(1).filter((point, index) => Date.parse(point.observedAt) - Date.parse(bffBody.points[index].observedAt) > 300_000).length : -1; const warnings = Array.isArray(bffBody?.points) ? bffBody.points.filter((point) => point.valid === false || point.outdated === true).length : -1;
    if (Object.values(statuses).some((status) => status !== 200) || pointCount < 2 || nestBody?.summary?.pointCount !== pointCount || !trackHtml.includes("data-vehicle-track-map") || !trackHtml.includes("track-from") || !trackHtml.includes("Europe/Kyiv") || !trackHtml.includes("Разрывы не интерполируются") || !detailsHtml.includes("История движения") || JSON.stringify(before.counts) !== JSON.stringify(after.counts) || before.digest !== after.digest || externalRequests !== 0) throw new Error("vehicle track smoke assertion");
    result = { statuses, naturalRange: { pointCount, gapCount: gaps, qualityWarningCount: warnings }, databaseBefore: before.counts, databaseAfter: after.counts, databaseDigestsUnchanged: true, network: { eQuGPS: externalRequests, telegram: 0, openFreeMap: 0, unexpected: externalRequests }, localPath: trackPath };
  } finally { await stop(web); await stop(api); await close(trap?.server); if (database) await database.end(); console.log(JSON.stringify(result)); }
}
void main().catch((error) => { console.error(error instanceof Error ? error.message : "vehicle track smoke failed"); process.exitCode = 1; });
