import assert from "node:assert/strict";
import test from "node:test";
import { DashboardContractError, parseDashboardVehiclesResponse } from "./dashboard-contract";
const valid = { serviceDate: "2026-08-05", timezone: "Europe/Kyiv", minimumDailyDistanceMeters: 500, positionFreshnessSeconds: 300, summary: { total: 1, online: 1, offline: 0, unknown: 0, freshPositions: 1, stalePositions: 0, withoutPosition: 0, belowMinimumDistance: 0, withoutDailyStat: 0 }, vehicles: [{ id: "local-id", name: "Vehicle", disabled: false, status: "online", externalLastUpdateAt: null, fixTime: "2026-08-05T12:00:00.000Z", speedKph: 20, positionValid: true, positionOutdated: false, positionFreshness: "fresh", dailyDistanceMeters: 1000, dailyDistanceSource: "runs", dailyDistanceQuality: "provisional", dailyDistanceStale: false, dailyDistanceDegraded: false, belowMinimumDistance: false }], generatedAt: "2026-08-05T12:01:00.000Z" };
test("validates the dashboard contract and strips unknown keys", () => { const parsed = parseDashboardVehiclesResponse({ ...valid, secret: "discarded" }); assert.equal("secret" in parsed, false); });
test("rejects invalid enums, timestamps and numeric values without preserving data", () => { for (const invalid of [{ ...valid, vehicles: [{ ...valid.vehicles[0], status: "other" }] }, { ...valid, generatedAt: "not-a-date" }, { ...valid, vehicles: [{ ...valid.vehicles[0], speedKph: -1 }] }]) assert.throws(() => parseDashboardVehiclesResponse(invalid), DashboardContractError); });
