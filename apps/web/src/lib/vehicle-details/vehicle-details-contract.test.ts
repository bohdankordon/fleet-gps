import assert from "node:assert/strict";
import test from "node:test";
import { VEHICLE_DETAILS_FIXTURE } from "./vehicle-details-fixture";
import { VehicleDetailsContractError, parseVehicleDetailsResponse } from "./vehicle-details-contract";
test("accepts disabled and connectivity independently from nullable position and today", () => { const value = parseVehicleDetailsResponse(VEHICLE_DETAILS_FIXTURE); assert.equal(value.activeAlerts.length, 2); assert.equal(value.recentEvents[1]?.type, "INACTIVITY"); const absent = parseVehicleDetailsResponse({ ...VEHICLE_DETAILS_FIXTURE, vehicle: { ...VEHICLE_DETAILS_FIXTURE.vehicle, disabled: true }, connectivity: "OFFLINE", currentState: null, today: null, activeAlerts: [], recentEvents: [] }); assert.equal(absent.currentState, null); assert.equal(absent.vehicle.disabled, true); assert.equal(absent.connectivity, "OFFLINE"); });
test("rejects invalid UUID, coordinate, timestamp, metrics, connectivity, missing disabled and sensitive passthrough", () => { const { disabled: _disabled, ...vehicleWithoutDisabled } = VEHICLE_DETAILS_FIXTURE.vehicle; const invalids = [{ ...VEHICLE_DETAILS_FIXTURE, vehicle: { ...VEHICLE_DETAILS_FIXTURE.vehicle, id: "bad" } }, { ...VEHICLE_DETAILS_FIXTURE, vehicle: vehicleWithoutDisabled }, { ...VEHICLE_DETAILS_FIXTURE, connectivity: "CONNECTED" }, { ...VEHICLE_DETAILS_FIXTURE, currentState: { ...VEHICLE_DETAILS_FIXTURE.currentState, position: { ...VEHICLE_DETAILS_FIXTURE.currentState.position, latitude: 91 } } }, { ...VEHICLE_DETAILS_FIXTURE, generatedAt: "bad" }, { ...VEHICLE_DETAILS_FIXTURE, today: { ...VEHICLE_DETAILS_FIXTURE.today, distanceMeters: -1 } }, { ...VEHICLE_DETAILS_FIXTURE, today: { ...VEHICLE_DETAILS_FIXTURE.today, source: "RAW" } }, { ...VEHICLE_DETAILS_FIXTURE, recentEvents: [{ ...VEHICLE_DETAILS_FIXTURE.recentEvents[0], notificationDeliveryStatus: "RAW" }] }, { ...VEHICLE_DETAILS_FIXTURE, secret: "no" }]; for (const value of invalids) assert.throws(() => parseVehicleDetailsResponse(value), VehicleDetailsContractError); });
