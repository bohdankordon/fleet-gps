import assert from "node:assert/strict";
import test from "node:test";
import { formatVehicleAge, formatVehicleDistance, formatVehicleDuration, formatVehicleSpeed, formatVehicleTimestamp, freshnessLabel, qualityLabel, sourceLabel } from "./vehicle-details-formatters";
test("formats vehicle metrics, durable Kyiv timestamps and future-safe age", () => { assert.equal(formatVehicleDistance(12345, "ru"), "12,3 км"); assert.equal(formatVehicleDuration(5400, "ru"), "1 ч 30 мин"); assert.equal(formatVehicleDuration(null, "ru"), "—"); assert.equal(formatVehicleSpeed(42, "ru"), "42 км/ч"); assert.equal(formatVehicleTimestamp("2026-08-10T12:00:01.000Z", "ru"), "2026-08-10, 15:00"); assert.equal(formatVehicleAge("2026-08-10T12:00:01.000Z", "2026-08-10T12:00:00.000Z", "ru"), "время позиции уточняется"); assert.equal(freshnessLabel("FRESH", "ru"), "Свежая"); assert.equal(qualityLabel("EXACT", "ru"), "Точные данные"); assert.equal(sourceLabel("RUNS", "ru"), "Данные пробегов"); });
