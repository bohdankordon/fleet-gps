import assert from "node:assert/strict";
import test from "node:test";
import { formatVehicleAge, formatVehicleDistance, formatVehicleDuration, formatVehicleSpeed, freshnessLabel, qualityLabel, sourceLabel } from "./vehicle-details-formatters";
test("formats vehicle metrics, Kyiv-facing labels and future-safe age", () => { assert.equal(formatVehicleDistance(12345), "12,3 км"); assert.equal(formatVehicleDuration(5400), "1 ч 30 мин"); assert.equal(formatVehicleDuration(null), "—"); assert.equal(formatVehicleSpeed(42), "42 км/ч"); assert.equal(formatVehicleAge("2026-08-10T12:00:01.000Z", "2026-08-10T12:00:00.000Z"), "время позиции уточняется"); assert.equal(freshnessLabel("FRESH"), "Свежая"); assert.equal(qualityLabel("EXACT"), "Точные данные"); assert.equal(sourceLabel("RUNS"), "Данные пробегов"); });
