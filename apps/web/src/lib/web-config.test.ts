import assert from "node:assert/strict";
import test from "node:test";
import { parseWebConfig, WebConfigurationError } from "./web-config";
test("normalizes an internal backend URL without changing input", () => { const env = { API_INTERNAL_BASE_URL: " https://api.example.test/base/ " }; assert.deepEqual(parseWebConfig(env), { apiInternalBaseUrl: "https://api.example.test/base", mapStyleUrl: null }); assert.equal(env.API_INTERNAL_BASE_URL, " https://api.example.test/base/ "); });
test("rejects unsafe backend URLs without serializing them", () => { const unsafe = "https://user:password@api.example.test/?token=value#fragment"; let error: unknown; try { parseWebConfig({ API_INTERNAL_BASE_URL: unsafe }); } catch (value) { error = value; } assert.ok(error instanceof WebConfigurationError); assert.equal(JSON.stringify(error).includes(unsafe), false); });
test("production map configuration accepts only the inventoried OpenFreeMap origin", () => { assert.equal(parseWebConfig({ API_INTERNAL_BASE_URL: "http://api.test", NEXT_PUBLIC_MAP_STYLE_URL: "https://tiles.openfreemap.org/styles/liberty" }).mapStyleUrl, "https://tiles.openfreemap.org/styles/liberty"); for (const value of ["http://tiles.openfreemap.org/styles/positron", "https://example.test/style", "https://token@tiles.openfreemap.org/style"]) assert.throws(() => parseWebConfig({ API_INTERNAL_BASE_URL: "http://api.test", NEXT_PUBLIC_MAP_STYLE_URL: value }), WebConfigurationError); });
