import type { AlertEventsListResponse } from "./alert-events-contract";
export function appendAlertEventsPage(current: AlertEventsListResponse, page: AlertEventsListResponse): AlertEventsListResponse { const known = new Set(current.items.map((item) => item.id)); return { items: [...current.items, ...page.items.filter((item) => { if (known.has(item.id)) return false; known.add(item.id); return true; })], nextCursor: page.nextCursor }; }
